1. When we are register using api some parameter(i.e username) are missing at api and when checked in the
database unhandled exeption appear.
2. For filters also mathematician exception comes out, while test with -ve parameters. 